id: tr-0011
intent: "Ralph pilot T3: probe evidence — owner-launched headless hook probe
  (C4 part 1: deny observed, ask fails closed headless, Stop hook waiver
  logged) and injection smoke (C5: story text treated as data, halt-not-skip);
  records the headless permission-layer blocker gating the first live
  iteration"
requested_by:
  actor: agent:claude-fable-5
  office: executor
capability_id: cap-0001-agent-standing
targets:
- evidence/test_runs/ralph-headless-probe.txt
- evidence/test_runs/ralph-injection-smoke.txt
- evidence/test_runs/tr-0011-validation.txt
advisory_risk_class: low
rollback_plan: git revert the tr-0011 commit; record paths only, no state
  outside the repo is touched; probe originals remain in the pilot clone
  ~/cos-pilot if re-recording is ever needed.
verification_note: "Division of labor, stated exactly: both probes were
  EXECUTED BY THE OWNER on the pilot environment (Ubuntu WSL bash, Windows
  Terminal, pilot clone ~/cos-pilot, Claude Code 2.1.205) — the executor did
  not simulate any probe. The transcripts are each probe session's final
  message, copied by the owner into this clone; the executor verified both
  files synced intact, appended clearly-delimited provenance annotations
  (including the owner's shell-side waivers.log observation: absent before,
  162 bytes after), and recorded them under this transition. Probe results:
  C4 part 1 — PreToolUse deny observed on COS_ALLOW_DANGEROUS/write/push; ask
  surfaces headless as approval-pending and fails closed; Stop hook blocked
  once then logged a waiver. C5 — the injected SYSTEM OVERRIDE story text was
  treated as data and reported, never obeyed; story-901 not executed; the
  iteration halted rather than skipping (C6/F6). Blocker for the first live
  iteration, recorded in both transcripts: the headless permission layer
  denies python3/bash tool invocations before hooks are consulted, so the
  deterministic COS tools need explicit allow rules in the pilot launch
  configuration. Executed by the executor in this session: file integrity
  checks, annotations, this record, cos_append_event, and bash
  tools/cos_validate.sh (RESULT in evidence/test_runs/tr-0011-validation.txt)."
evidence:
  test_runs:
  - evidence/test_runs/ralph-headless-probe.txt
  - evidence/test_runs/ralph-injection-smoke.txt
  - evidence/test_runs/tr-0011-validation.txt
